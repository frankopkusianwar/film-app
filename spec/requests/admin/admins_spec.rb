 require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/admin/admins", type: :request do
  # Admin::Admin. As you add validations to Admin::Admin, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      Admin::Admin.create! valid_attributes
      get admin_admins_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      admin = Admin::Admin.create! valid_attributes
      get admin_admin_url(admin_admin)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_admin_admin_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "render a successful response" do
      admin = Admin::Admin.create! valid_attributes
      get edit_admin_admin_url(admin_admin)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new Admin::Admin" do
        expect {
          post admin_admins_url, params: { admin_admin: valid_attributes }
        }.to change(Admin::Admin, :count).by(1)
      end

      it "redirects to the created admin_admin" do
        post admin_admins_url, params: { admin_admin: valid_attributes }
        expect(response).to redirect_to(admin_admin_url(Admin::Admin.last_admin))
      end
    end

    context "with invalid parameters" do
      it "does not create a new Admin::Admin" do
        expect {
          post admin_admins_url, params: { admin_admin: invalid_attributes }
        }.to change(Admin::Admin, :count).by(0)
      end

      it "renders a successful response (i.e. to display the 'new' template)" do
        post admin_admins_url, params: { admin_admin: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested admin_admin" do
        admin = Admin::Admin.create! valid_attributes
        patch admin_admin_url(admin_admin), params: { admin_admin: new_attributes }
        admin.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the admin_admin" do
        admin = Admin::Admin.create! valid_attributes
        patch admin_admin_url(admin_admin), params: { admin_admin: new_attributes }
        admin.reload
        expect(response).to redirect_to(admin_admin_url(admin))
      end
    end

    context "with invalid parameters" do
      it "renders a successful response (i.e. to display the 'edit' template)" do
        admin = Admin::Admin.create! valid_attributes
        patch admin_admin_url(admin_admin), params: { admin_admin: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested admin_admin" do
      admin = Admin::Admin.create! valid_attributes
      expect {
        delete admin_admin_url(admin_admin)
      }.to change(Admin::Admin, :count).by(-1)
    end

    it "redirects to the admin_admins list" do
      admin = Admin::Admin.create! valid_attributes
      delete admin_admin_url(admin_admin)
      expect(response).to redirect_to(admin_admins_url)
    end
  end
end
